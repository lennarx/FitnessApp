import type { Metadata } from "next";

export const metadata: Metadata = { title: "Entrenar" };

export default function SessionLogLayout({ children }: { children: React.ReactNode }) {
  return children;
}
