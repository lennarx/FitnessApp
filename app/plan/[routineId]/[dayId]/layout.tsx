import type { Metadata } from "next";

export const metadata: Metadata = { title: "Día" };

export default function RoutineDayLayout({ children }: { children: React.ReactNode }) {
  return children;
}
