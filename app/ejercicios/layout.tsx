import type { Metadata } from "next";

export const metadata: Metadata = { title: "Ejercicios" };

export default function EjerciciosLayout({ children }: { children: React.ReactNode }) {
  return children;
}
